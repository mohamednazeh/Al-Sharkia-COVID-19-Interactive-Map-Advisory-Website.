expSys.attrs.state = "plus symptoms";
// expSys.attrs.patient.state = "plus symptoms";
// Define it when there's a complete for isolated case >> 2
// expSys.attrs.patient.isStableCase = true;

// Back Missions
// )+1 Define it when there's a complete for isolated case >> 2
// expSys.attrs.patient.isStableCase = true;

